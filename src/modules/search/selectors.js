export function getSearch(state) {
  return state?.search;
}

export function getFilms(state) {
  return state?.films;
}

export function getLoading(state) {
  return state?.loading;
}

export function getError(state) {
  return state?.error;
}

export function getTyping(state) {
  return state?.typing;
}
